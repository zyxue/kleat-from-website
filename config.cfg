[commands]
polyA: -a ${aln} -c ${fa} -g ${genome} -o ${out} -b ${bam} -r -e --use_tmp
polyA-filter: -x ${results} -o ${out} -n 2 -k ${lib}-CLEAT '${lib} polyA cleavage sites' 

[memory]
polyA: 8G
polyA-filter: 5G

[project_name]
topdir: /path/to/working/directory
reference: hg19
